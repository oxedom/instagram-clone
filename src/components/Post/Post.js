const Post = (props) => {

    const {url, text, uid, } = props
    const fetchUser = (id) => 
    {
        
    } 
    return ( <div>

        <img alt={text }src={url} /> 
        
    </div>  );
}
 
export default Post;